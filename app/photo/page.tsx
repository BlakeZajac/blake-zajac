"use client";

import Container from "@/components/container";
import Section from "@/components/section";

interface PhotoPageProps {}

const PhotoPage: React.FC<PhotoPageProps> = () => {
  return (
    <Section>
      <Container>Photo Page</Container>
    </Section>
  );
};

export default PhotoPage;
